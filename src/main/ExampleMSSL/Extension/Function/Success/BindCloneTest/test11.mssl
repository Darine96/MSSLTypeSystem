fn f1(mut x: &'a mut trc<box<int>>, mut y: int)->0 int{
       print!(*** x);
       y;
}

{
    let mut x = trc(0);
    let mut b = trc(box(0));
    let mut c = &mut b;
    print!(***c);
   // f1(&mut b, 1);

}